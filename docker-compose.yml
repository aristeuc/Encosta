services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: obra
      POSTGRES_PASSWORD: obra
      POSTGRES_DB: obra_tracker
    ports:
      - "5432:5432"
    volumes:
      - obra_tracker_db:/var/lib/postgresql/data

volumes:
  obra_tracker_db:
